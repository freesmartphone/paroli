# Paroli configuration file
#
# This config is used when paroli is run from within the source folder
# this config is aimed to provide a development environment

[plugins]
path = ../services/:../applications/

[services]
defaults = Alarm:Fallback, Audio:Fallback, Buttons:Fallback, Config:Fallback, Contacts:Fallback, Dialog:Fallback, Display:Fallback, IdleNotifier:Fallback, Gprs:Fallback, GSM:Fallback, Messages:Fallback, Power:Fallback, Prefs:Fallback, SIM:Fallback, SMS:Fallback, Sounds:Fallback, SysTime:Fallback, Usage:Fallback, Ussd:Fallback, Vibrator:Fallback, Wifi:Fallback, 

[autolaunch]
application = Paroli-Launcher2

[standalone]
activated = True

[advanced-mode]
activated = True

[dbus]
activated = True

[settings]
activated = True

[error_messages]
activated = True
